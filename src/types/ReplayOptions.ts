export enum ReplayOptions {
  /**
   * Loop replay.
   */
  RIPOPT_LOOP = 1,

  /**
   * Download missing skins.
   */
  RIPOPT_SKINS = 2,

  /**
   * Have LFS calculate full physics during replay seek.
   */
  RIPOPT_FULL_PHYS = 4,
}
